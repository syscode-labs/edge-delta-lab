# Execution record

Publication copies use redacted paths and infrastructure labels. See the
[redaction and integrity note](REDACTION.md) before interpreting historical records.

Prepared 2026-09-17. Host execution: Linux amd64, Go 1.23.2.

- `make check`: PASS (Go tests, `go vet`, race detector, four Python normalization tests).
- `python3 scripts/watch_smoke.py`: PASS. The same live agent PID staged both successive signed channel releases.
- `scripts/demo.py --size-mib 16 --rate-kbit 5000`: PASS. Real HTTP transfers and actual SIGKILL/restart; see `results.json` and `RESULTS.md`.
- Linux arm64 and macOS arm64 binaries: successfully cross-compiled, **not runtime-tested**.
- Docker import/run smoke: **NOT RUN**; no Docker CLI/daemon was installed in the authoring environment.
- Docker Compose topology: **NOT RUN** for the same reason.
- Hosted CI: workflow included, **NOT EXECUTED** as part of this delivery.
- Physical power removal / abrupt VM power-off: **NOT TESTED**. SIGKILL does not establish storage-device durability.
- 1–2 GiB or production images: **NOT BENCHMARKED**. The scripts expose those sizes; recorded results use 16 MiB high-entropy fixtures.

No publisher private key is included. `publisher.pub` and signed release envelopes are retained to make release-metadata evidence verifiable. Chunk payloads and staged images are generated by the demo and excluded from this source bundle.

`TESTED_SOURCE_SHA256SUMS` identifies the source files corresponding to these tests. `demo-logs` contains actual agent JSONL events; the process that was killed has no successful final summary. The `results.json` cold scenario totals come from origin counters spanning both agent processes, whereas its nested agent summary is only the resumed invocation.
